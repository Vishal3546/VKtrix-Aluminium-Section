spring:
  datasource:
    # Supavisor session-mode connection string (port 6543)
    url: jdbc:postgresql://aws-0-ap-south-1.pooler.supabase.com:6543/postgres?sslmode=require&prepareThreshold=0
    username: postgres.zslfkwunzsgejajtfpeg
    password: Kaushalp@8095
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      idle-timeout: 30000
      pool-name: SupabaseHikariCP

  jpa:
    hibernate:
      ddl-auto: update # Use Hibernate to automatically create missing tables
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
        tenant_identifier_resolver: com.app.config.TenantIdentifierResolver

  security:
    oauth2:
      resourceserver:
        jwt:
          # Supabase issues JWTs. Spring Boot acts as a Resource Server.
          issuer-uri: https://zslfkwunzsgejajtfpeg.supabase.co/auth/v1
          jwk-set-uri: https://zslfkwunzsgejajtfpeg.supabase.co/auth/v1/.well-known/jwks.json
