spring:
  application:
    name: aluminium-app
  profiles:
    active: dev
  flyway:
    enabled: true
    baseline-on-migrate: true

server:
  port: 8080

logging:
  file:
    name: application.log
  level:
    root: INFO
    web: DEBUG
